// LazyContent.jsx
import React from "react";

const LazyContent = () => {
  return (
    <div style={{ marginTop: "20px", fontSize: "18px", color: "blue" }}>
      This content was loaded lazily using React.lazy!
    </div>
  );
};

export default LazyContent;
